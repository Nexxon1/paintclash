# IDE / Editor
.idea/

# Tool-Artefakte
.playwright/

# Claude Code (nur lokale, nicht geteilte Einstellungen)
.claude/settings.local.json

# Node / Build (für die spätere Umsetzung)
node_modules/
dist/
build/
*.log

# Ausnahme: Bauphasen-Ticket-Ordner ist Doku (kollidiert nur namentlich mit build/)
!.scratch/draw-race/build/

# Env / Secrets
.env
.env.*
!.env.example
