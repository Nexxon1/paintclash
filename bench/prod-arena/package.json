{
  "name": "@paintclash/bench-prod-arena",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run pilot",
    "bench": "vitest run load"
  },
  "dependencies": {
    "@paintclash/protocol": "workspace:*",
    "@paintclash/shared": "workspace:*",
    "@paintclash/sim-client": "workspace:*",
    "@paintclash/sim-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
